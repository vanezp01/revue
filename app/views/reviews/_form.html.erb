<%= simple_form_for([@flick, @flick.reviews.build]) do |f| %>
	<div id="rating-form">
		<%= f.input :rating %>
		<label> Rating </label>
	</div>
	<%= f.input :comment %>
	<%= f.button :submit, :class => "btn-custom2" %>
<% end %>
